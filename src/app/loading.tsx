
export default function Loading() {
  // This component is shown while the page (Server Component) is fetching data.
  // We can return null because our 'Preloader' in layout.tsx will handle the visual loading state.
  return null;
}
